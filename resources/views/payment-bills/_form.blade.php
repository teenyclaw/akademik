<div class="mb-4"><x-input-label value="Siswa" /><select name="student_id" class="mt-1 block w-full border-gray-300 rounded-md" required>@foreach($students as $s)<option value="{{ $s->id }}" @selected(old('student_id', $item->student_id ?? '') == $s->id)>{{ $s->name }}</option>@endforeach</select></div>
<div class="mb-4"><x-input-label value="Jenis Biaya" /><select name="fee_type_id" class="mt-1 block w-full border-gray-300 rounded-md" required>@foreach($feeTypes as $f)<option value="{{ $f->id }}" @selected(old('fee_type_id', $item->fee_type_id ?? '') == $f->id)>{{ $f->name }}</option>@endforeach</select></div>
<div class="mb-4"><x-input-label for="amount" value="Nominal" /><x-text-input id="amount" name="amount" type="number" step="0.01" class="mt-1 block w-full" value="{{ old('amount', $item->amount ?? '') }}" required /></div>
<div class="mb-4"><x-input-label for="due_date" value="Jatuh Tempo" /><x-text-input id="due_date" name="due_date" type="date" class="mt-1 block w-full" value="{{ old('due_date', optional($item->due_date)->format('Y-m-d')) }}" /></div>
<div class="mb-4"><x-input-label for="period" value="Periode" /><x-text-input id="period" name="period" class="mt-1 block w-full" value="{{ old('period', $item->period ?? '') }}" /></div>
