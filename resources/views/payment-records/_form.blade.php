<div class="mb-4"><x-input-label value="Tagihan" /><select name="payment_bill_id" class="mt-1 block w-full border-gray-300 rounded-md" required>@foreach($bills as $b)<option value="{{ $b->id }}" @selected(old('payment_bill_id', $item->payment_bill_id ?? '') == $b->id)>{{ $b->student?->name }} - {{ $b->amount }}</option>@endforeach</select></div>
<div class="mb-4"><x-input-label for="amount" value="Nominal Bayar" /><x-text-input id="amount" name="amount" type="number" step="0.01" class="mt-1 block w-full" value="{{ old('amount', $item->amount ?? '') }}" required /></div>
<div class="mb-4"><x-input-label for="payment_date" value="Tanggal Bayar" /><x-text-input id="payment_date" name="payment_date" type="date" class="mt-1 block w-full" value="{{ old('payment_date', optional($item->payment_date)->format('Y-m-d') ?? now()->format('Y-m-d')) }}" required /></div>
<div class="mb-4"><x-input-label for="method" value="Metode" /><x-text-input id="method" name="method" class="mt-1 block w-full" value="{{ old('method', $item->method ?? '') }}" /></div>
